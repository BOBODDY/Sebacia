<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:background="@color/gold3"
    tools:context="com.derma.sebacia.ui.CameraActivity">

    <FrameLayout
        android:id="@+id/camera_preview"
        android:layout_width="fill_parent"
        android:layout_height="400dp"
        android:layout_marginBottom="25dp">
        <ImageView
            android:id="@+id/camera_overlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/faceoval"/>
    </FrameLayout>
    
    
    <ProgressBar
        android:id="@+id/camera_loading"
        android:indeterminate="true"
        android:visibility="invisible"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/camera_capture"
        android:text="@string/camera_take_picture"
        android:layout_width="125dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/camera_preview"
        android:layout_centerHorizontal="true"
        android:background="@drawable/mybutton"
        android:textColor="@color/gold1"
        android:layout_marginBottom="50dp"/>

    <Button
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="@string/camera_diagnose"
        android:id="@+id/camera_diagnose"
        android:background="@drawable/mybutton"
        android:textColor="@color/gold1"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
